"""
Configuration

CrewAI-specific configuration for the Paper Finder system.

Configuration includes:
- Agent settings (LLM models, temperature, etc.)
- Tool settings (timeouts, retry logic, etc.)
- Crew settings (memory, caching, etc.)
- API keys and external service configuration

Inherits from ai2i.config for consistency with mabool implementation.
"""
